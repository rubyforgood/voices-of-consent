<%= form_with(model: meeting_type, local: true) do |form| %>
  <% if meeting_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(meeting_type.errors.count, "error") %> prohibited this meeting_type from being saved:</h2>

      <ul>
      <% meeting_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
