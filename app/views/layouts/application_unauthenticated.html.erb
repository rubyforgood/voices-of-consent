<!DOCTYPE html>
<html>
  <head>
    <%= render 'shared/head' %>
  </head>

  <body>

    <div class="container" id="react-root">
      <%= yield :title %>
      <%= yield :breadcrumbs %>
      <%= yield %>
    </div>

    <%= render 'shared/footer' %>
  </body>
</html>
