<!DOCTYPE html>
<html>
  <head>
    <%= render 'shared/head' %>
  </head>

  <body>
    <%= render 'shared/navigation' %>

    <div class="container">
      <%= yield :title %>
      <%= yield :breadcrumbs %>
      <%= yield %>
    </div>

    <div class="container" id="react-root"></div>

    <%= render 'shared/footer' %>
  </body>
</html>
